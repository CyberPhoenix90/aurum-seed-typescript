{
    "name": "aurum-seed-typescript",
    "version": "0.0.0",
    "description": "",
    "private": true,
    "scripts": {
        "backend": "cd server && nodemon --watch 'src/**/*.mts' --exec 'tsc && node dist/index.mjs'",
        "dev": "vite",
        "build": "vite build && cd server && tsc",
        "host": "npm run build && cd server && node dist/index.mjs",
        "test:client": "vitest --config ./client/vitest.config.ts",
        "test:server": "vitest --config ./server/vitest.config.ts",
        "coverage": "vitest run --coverage"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "@vitest/ui": "^0.30.1",
        "happy-dom": "^9.9.2",
        "nodemon": "^2.0.22",
        "typescript": "^5.0.4",
        "vite": "^4.3.3",
        "vitest": "^0.30.1"
    },
    "dependencies": {
        "@emotion/css": "^11.10.6",
        "aurum-canvas": "^0.0.31",
        "aurum-components": "^0.3.1",
        "aurumjs": "^0.8.16"
    }
}
